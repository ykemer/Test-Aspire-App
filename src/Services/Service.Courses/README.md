# Service.Courses

The Service.Courses is a microservice responsible for managing courses and classes. It exposes gRPC endpoints for course and class operations. It uses PostgreSQL for data persistence, Redis for caching, and MassTransit for inter-service messaging.
